services:
  spring_boot_app:
    build:
      dockerfile: Dockerfile

    container_name: sensor_log_app
    environment:
      SPRING_PROFILES_ACTIVE: prod
    ports:
      - "8080:8080"

    restart: unless-stopped